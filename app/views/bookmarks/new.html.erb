<h1>New Bookmark</h1>
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment, label: 'Comment' %>

  <div class="actions">
    <%= f.submit 'Create Bookmark' %>
  </div>
<% end %>

<%= link_to 'Back', @list %>
